# Scripts

This skill intentionally keeps executable helper code under `assets/matlab/` because the files are meant to be copied into a MATLAB project or `.MBD_agent/scripts/` by the AI agent when needed.
